import { consume, Consumer } from './consumer'
import { Model, createModel } from './model'
import Collection from './collection'

export { consume, Consumer, Model, createModel, Collection }

export default consume
